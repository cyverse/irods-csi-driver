globalConfig:
  secret:
    stringData:
      client: "irodsfuse"
      host: "data.cyverse.org"
      port: "1247"
      zone: "iplant"
      mountPathWhitelist: "/iplant/home"
controllerService:
  csiProvisioner:
    extraArgs:
      - --timeout=5m
      - --v=5
      - --leader-election
