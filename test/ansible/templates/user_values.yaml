globalConfig:
  secret:
    stringData:
      client: "irodsfuse"
      host: "data.cyverse.org"
      port: "1247"
      zone: "iplant"
      poolEndpoint: "{{ irodsfs_pool_endpoint }}"
      mountPathWhitelist: "/iplant/home"
